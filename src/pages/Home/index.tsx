
import "./index.scss";

import { useSetup } from "./hooks";


type HomeProps = {
  
};

function Home(props: HomeProps) {
  const {} = props;
  const {} = useSetup();

  return (
    <div className="home">
      Home
    </div>
  );
}

export default Home;
