
import "./index.scss";

import { useSetup } from "./hooks";


type DeckEditProps = {
  
};

function DeckEdit(props: DeckEditProps) {
  const {} = props;
  const {} = useSetup();

  return (
    <div className="deck-edit">
      DeckEdit
    </div>
  );
}

export default DeckEdit;
