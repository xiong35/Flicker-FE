
import "./index.scss";

import { useSetup } from "./hooks";


type LoginProps = {
  
};

function Login(props: LoginProps) {
  const {} = props;
  const {} = useSetup();

  return (
    <div className="login">
      Login
    </div>
  );
}

export default Login;
